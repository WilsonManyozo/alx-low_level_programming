#include "main.h"
#include <stdio.h>

int _sqrt(int n, int i);

/**
 * _sqrt_recursion - Returns the natural sqrt of a num
 * @n: Num to calculate the natural sqrt
 * Return: The natural sqrt
 */

int _sqrt_recursion(int n)
{
	return (_sqrt(n, 1));

	if (_sqrt > n)
		return (-1);

	if (_sqrt == n)

	return (_sqrt(n, i + 1));
}
